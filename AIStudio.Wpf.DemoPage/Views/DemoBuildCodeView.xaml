<UserControl x:Class="AIStudio.Wpf.DemoPage.Views.DemoBuildCodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AIStudio.Wpf.DemoPage.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <WrapPanel>
            <TextBlock Margin="3" VerticalAlignment="Center" Style="{StaticResource TextBlockBaseStyle}">Demo名称</TextBlock>
            <TextBox Width="200" Margin="3" Text="{Binding DemoName}" Style="{StaticResource TextBoxBaseStyle}"></TextBox>
            <Button Width="50" Height="28" Margin="3" Command="{Binding GenerateCommand}" Style="{StaticResource ButtonBaseStyle}">生成</Button>
        </WrapPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Types}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ViewModel" Binding="{Binding FullName}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

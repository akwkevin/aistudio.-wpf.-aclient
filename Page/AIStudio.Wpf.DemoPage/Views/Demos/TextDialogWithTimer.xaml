<Border x:Class="AIStudio.Wpf.DemoPage.Views.TextDialogWithTimer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:util="https://astudio.github.io/utilcontrol"
        CornerRadius="10"
        Width="400"
        Height="247"
        Background="{DynamicResource RegionBrush}">
    <util:SimplePanel>
        <Viewbox Width="80" Height="80">
            <TextBlock TextWrapping="Wrap" Style="{StaticResource TextBlockLargeBold}" Text="PleaseWait"/>
        </Viewbox>
        <Button Name="ButtonClose" Width="22" Height="22" Command="{Binding CloseCmd}" Style="{StaticResource ButtonIcon}" Foreground="{DynamicResource AccentColorBrush}" util:IconElement.Geometry="{StaticResource ErrorGeometry}" Padding="0" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,4,4,0"/>
        <util:CircleProgressBar Name="ProgressBarTimer" ShowText="False" Width="120" Height="120"/>
    </util:SimplePanel>
</Border>

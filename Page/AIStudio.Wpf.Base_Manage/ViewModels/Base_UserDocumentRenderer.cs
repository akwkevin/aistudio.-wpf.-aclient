using AIStudio.Core.Helpers;
using System;
using System.Collections.Generic;
using System.Text;
using System.Windows.Documents;

namespace AIStudio.Wpf.Base_Manage.ViewModels
{
    class Base_UserDocumentRenderer : IDocumentRenderer
    {
        public void Render(FlowDocument doc, object data)
        {
            //TableRowGroup groupDetails = doc.FindName("rowsDetails") as TableRowGroup;
            ////TableRowGroup groupOverview = doc.FindName("rowsOverview") as TableRowGroup;

            //Style styleCell = doc.Resources["BorderedCell"] as Style;
            //foreach (DeviceHourlyDataInfo item in ((DeviceHourlyPrintResult)data).DeviceHourlyDataInfo)
            //{
            //    TableRow row = new TableRow();

            //    TableCell cell = new TableCell(new Paragraph(new Run(item.DeviceTreeItemViewModel.Name)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run(item.Unit)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData0 != null) ? item.HourlyData0.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData1 != null) ? item.HourlyData1.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData2 != null) ? item.HourlyData2.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData3 != null) ? item.HourlyData3.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData4 != null) ? item.HourlyData4.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData5 != null) ? item.HourlyData5.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData6 != null) ? item.HourlyData6.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData7 != null) ? item.HourlyData7.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData8 != null) ? item.HourlyData8.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData9 != null) ? item.HourlyData9.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData10 != null) ? item.HourlyData10.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData11 != null) ? item.HourlyData11.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData12 != null) ? item.HourlyData12.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData13 != null) ? item.HourlyData13.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData14 != null) ? item.HourlyData14.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData15 != null) ? item.HourlyData15.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData16 != null) ? item.HourlyData16.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData17 != null) ? item.HourlyData17.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData18 != null) ? item.HourlyData18.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData19 != null) ? item.HourlyData19.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData20 != null) ? item.HourlyData20.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData21 != null) ? item.HourlyData21.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData22 != null) ? item.HourlyData22.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    cell = new TableCell(new Paragraph(new Run((item.HourlyData23 != null) ? item.HourlyData23.Value.ToString("f3") : null)));
            //    cell.Style = styleCell;
            //    row.Cells.Add(cell);

            //    groupDetails.Rows.Add(row);
            //}
        }
    }
}

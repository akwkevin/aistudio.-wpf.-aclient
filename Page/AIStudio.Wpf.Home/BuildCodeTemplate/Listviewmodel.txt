using AIStudio.Wpf.%areaName%.Views;
using AIStudio.Wpf.BasePage.ViewModels;
using AIStudio.Wpf.Entity.DTOModels;
using System.Threading.Tasks;

namespace AIStudio.Wpf.%areaName%.ViewModels
{
    public class %entityName%ViewModel : BaseListWithEditViewModel<%entityName%DTO, %entityName%Edit>
    {
        public %entityName%ViewModel()
        {
		    Area = "%areaName%";
        }      
        
        protected override async Task GetData()
        {
            await base.GetData();
        }

        protected override void Edit(%entityName%DTO para = null)
        {
            base.Edit(para);
        }

        protected override async Task Delete(string id = null)
        {
            await base.Delete(id);
        }

        protected override void Print()
        {
            base.Print(Data);
        }

        protected override void Search(object para = null)
        {
            base.Search(para);
        }     
    }
}
